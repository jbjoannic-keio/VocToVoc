#pragma once
#include "portaudio.h"
#include <fstream>

#include <chrono>  // for std::chrono::seconds
#include <thread>  // for std::this_thread::


class Record
{
public:
	static int main(void);

}; // class Record